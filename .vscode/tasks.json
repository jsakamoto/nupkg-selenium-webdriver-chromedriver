{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BuildPackage",
            "type": "shell",
            "command": "dotnet",
            "args": [
                "pack",
                "${workspaceFolder}/buildTools/Selenium.WebDriver.ChromeDriver.csproj"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}